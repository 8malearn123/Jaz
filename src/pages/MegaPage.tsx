import { MegaAccount } from './mega/MegaAccount'

/**
 * The Mega Business · Export portal — a dedicated workspace for a large
 * wholesale/export B2B account: pallet catalog, bulk orders, cold-chain export
 * shipments, credit and compliance. Parallel to /business but export-first.
 */
export function MegaPage() {
  return <MegaAccount />
}
